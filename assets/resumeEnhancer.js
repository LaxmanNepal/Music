const KEY='southMusicResume';let current=null;const audio=()=>document.querySelector('#south-audio');
function get(){try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}}
function set(v){localStorage.setItem(KEY,JSON.stringify(v))}
function render(){const main=document.querySelector('main');const hero=main?.querySelector('.hero');const old=document.querySelector('[data-resume-rail]');const r=get();if(!main||!hero||!r?.songId)return;if(old)old.remove();const sec=document.createElement('section');sec.className='section';sec.dataset.resumeRail='1';sec.innerHTML=`<div class="section-head"><h2>Continue Listening</h2><small>${Math.round((r.position/(r.duration||1))*100)}% played</small></div><div class="rail"><article class="card"><img class="art" src="${r.artwork||''}" alt=""><div class="song-title">${r.title||'Continue listening'}</div><div class="meta">Resume at ${fmt(r.position)} · ${fmt(r.duration)}</div><div class="actions"><button class="play-btn" data-play="${r.songId}">▶</button><button class="icon-btn" data-clear-resume>×</button></div></article></div>`;hero.after(sec);sec.querySelector('[data-clear-resume]').onclick=()=>{localStorage.removeItem(KEY);render()}}
function fmt(n){n=Math.max(0,Math.floor(n||0));return `${Math.floor(n/60)}:${String(n%60).padStart(2,'0')}`}
document.addEventListener('click',e=>{const b=e.target.closest('[data-play]');if(!b)return;current=b.dataset.play;const card=b.closest('.card,.row');const img=card?.querySelector('img')?.src||'';const title=card?.querySelector('.song-title')?.textContent?.trim()||'';setTimeout(()=>{const a=audio();const r=get();if(a&&r?.songId===current&&r.position>3&&r.position<(a.duration||r.duration||1)-5){a.currentTime=r.position}},350);const r=get()||{};set({...r,songId:current,title,artwork:img,position:r.songId===current?r.position:0,duration:r.songId===current?r.duration:0})});
setInterval(()=>{const a=audio();if(!a||!current||!isFinite(a.currentTime)||a.currentTime<1)return;const r=get()||{};set({...r,songId:current,position:a.currentTime,duration:a.duration||r.duration||0});render()},15000);
document.addEventListener('ended',()=>{if(current){localStorage.removeItem(KEY);render()}});new MutationObserver(render).observe(document.documentElement,{childList:true,subtree:true});render();
