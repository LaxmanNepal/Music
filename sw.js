const ROOT=new URL('./',self.location).pathname;const CACHE='south-music-v4';const SHELL=[ROOT,ROOT+'index.html',ROOT+'assets/styles.css',ROOT+'assets/a11y.css',ROOT+'assets/boot.js',ROOT+'assets/app.js',ROOT+'assets/recommendationEngine.js',ROOT+'manifest.json',ROOT+'icon.svg',ROOT+'data/songs.json',ROOT+'data/artists.json',ROOT+'data/albums.json',ROOT+'data/genres.json',ROOT+'data/languages.json',ROOT+'data/moods.json',ROOT+'data/trending.json',ROOT+'data/featured.json'];self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL)).then(()=>self.skipWaiting())));self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==location.origin||e.request.method!=='GET')return;e.respondWith(caches.match(e.request).then(c=>c||fetch(e.request).then(r=>{if(r.ok)caches.open(CACHE).then(x=>x.put(e.request,r.clone()));return r}).catch(()=>caches.match(ROOT))))});